@model IEnumerable<AppMartin.Models.Pregunta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RespuestaCorrecta.Contenido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contenido)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RespuestaCorrecta.Contenido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contenido)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PreguntaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PreguntaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PreguntaID })
        </td>
    </tr>
}

</table>
